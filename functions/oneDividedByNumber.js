const oneDividedByNumber = () => {
  document.getElementById("numbers-results").textContent =
    1 / parseFloat(document.getElementById("numbers-results").textContent);
};

export default oneDividedByNumber;
